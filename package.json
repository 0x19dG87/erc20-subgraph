{
  "name": "erc20-approvals",
  "version": "0.0.5",
  "description": "Subgraph for indexing all ERC20 token approvals",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service 0x19dG87/erc20-approvals"
  },
  "license": "MIT",
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.21.1",
    "@graphprotocol/graph-ts": "^0.20.0"
  }
}